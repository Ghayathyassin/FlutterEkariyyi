<?xml version="1.0" encoding="utf-8"?>
<!-- Paths the FileProvider may share. `cache` covers the app cache dir where
     the receipt PDF is written before being opened. -->
<paths>
    <cache-path name="cache" path="." />
    <files-path name="files" path="." />
    <external-files-path name="ext_files" path="." />
</paths>
